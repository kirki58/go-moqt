`message` package provides the message serialization/desiralization logic for struct types defined in `model` package.

implement the functions that will read and write the MOQT messages according to the Draft-15 binary format specification.